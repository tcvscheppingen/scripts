# Scripts

This is a collection of scripts to make life a little bit better.

- `blue-gamma` - uses arandr to reduce blue colors on the display. 
- `blue-gamma-reset` - uses arandr to reset display colors to default. 

You can use these scripts in a cronjob to automate blue light filtering.

_More scripts will follow..._
